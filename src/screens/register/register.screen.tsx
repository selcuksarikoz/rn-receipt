import { Text } from "react-native";
import { AppSafeAreaView } from "../../components";

export function AppRegisterScreen() {
  return (
    <AppSafeAreaView>
      <Text>register</Text>
    </AppSafeAreaView>
  )
}