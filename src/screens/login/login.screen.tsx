import { Text } from "react-native";
import { AppSafeAreaView } from "../../components";

export function AppLoginScreen() {
  return (
    <AppSafeAreaView>
      <Text>Login</Text>
    </AppSafeAreaView>
  )
}