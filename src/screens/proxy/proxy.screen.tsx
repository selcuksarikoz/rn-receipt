import { Text } from "react-native";
import { AppSafeAreaView } from "../../components";

export function AppProxyScreen() {
  return (
    <AppSafeAreaView>
      <Text>Proxy</Text>
    </AppSafeAreaView>
  )
}